[project]
name = "hexlet-code"
version = "0.1.0"
description = "A package to compare files"
authors = [
    {name = "RatiborM", email = "r_morkvin@vk.com"}
]

[tool.poetry]
name = "python-project-50"
version = "0.1.0"
description = "Вычислитель отличий"
authors = ["Роман Морквин <r_morkvin@.com>"]

[tool.poetry.dependencies]
python = ">=3.8.1,<4.0"
pyyaml = "^6.0.1"


[tool.poetry.group.dev.dependencies]
flake8 = "^7.0.0"
pytest = "^8.0.0"
pytest-cov = "^4.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
gendiff = "gendiff.scripts.gendiff:main"